# Exercise 1 - The requirements

Dad, I used the code in frac.py and I lost all sorts of points. For example, I produced one answer of (6, 12). The teacher wanted (1, 2) instead. can you change the code to put answers in lowest terms?

Also, the teacher told us to never put a negatice number in the denominator. So, you'd never write (2. -3). Instead you'd write (-2, 3). Also, (-2, -3) should just written as (2, 3).

and last, but not least, what is with tht code you wrote ? I can hardly read anything that's going on in there with all of that tuple indexing.

to fix all of these problems, you decidde to introduce a few helper functions. A make_frac() function will be used to construct the tuples and put things in lowest terms. To hide tuples indexing, you'll use numerator() and denominator() functions.
